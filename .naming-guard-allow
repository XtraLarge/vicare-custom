# naming-guard Allowlist — eine erweiterte Regex (ERE) je Zeile.
# Gematcht wird gegen 'datei:zeile:inhalt'. Nur fuer bewusste, gepruefte Ausnahmen.
# Beispiel (auskommentiert):
# ^README\.md:.*10\.10\.0\.1
